---
import Base from '../layouts/Base.astro';
import Timeline from '../components/Timeline.astro';
import Sky from '../components/Sky.astro';
import SolarSystem from '../components/SolarSystem.astro';

const DAY_MS = 86_400_000;
const now = new Date();
const year = now.getUTCFullYear();
const yearStart = Date.UTC(year, 0, 1);
const totalDays = Math.round((Date.UTC(year + 1, 0, 1) - yearStart) / DAY_MS);
const today = Date.UTC(year, now.getUTCMonth(), now.getUTCDate());
const dayOfYear = Math.round((today - yearStart) / DAY_MS) + 1;
---

<Base title="yearofmax" description="" ground="night">
  <Sky />
  <SolarSystem />
  <Timeline />

  <section class="hero">
    <p class="counter" data-counter>{dayOfYear}</p>
    <p class="label" data-counter-total>{totalDays}</p>
    <h1 class="heading"></h1>
    <p class="measure"></p>
  </section>
</Base>

<style>
  /* Above the timeline, which is fixed at z-index 0. */
  .hero {
    position: relative;
    z-index: 1;
    padding-top: clamp(1.25rem, 4vh, 3rem);
    padding-bottom: clamp(1.5rem, 5vh, 4rem);
  }

  .hero h1,
  .hero .measure {
    margin-top: clamp(0.75rem, 2.5vh, 2rem);
  }
</style>

<script>
  const DAY_MS = 86_400_000;

  /**
   * Only the year length is corrected here. The day number itself is driven by
   * the timeline, which owns it — see Timeline.astro.
   */
  function refresh(): void {
    const total = document.querySelector<HTMLElement>('[data-counter-total]');
    if (!total) return;

    const now = new Date();
    const year = now.getUTCFullYear();
    const yearStart = Date.UTC(year, 0, 1);
    total.textContent = String(Math.round((Date.UTC(year + 1, 0, 1) - yearStart) / DAY_MS));
  }

  document.addEventListener('astro:page-load', refresh);
</script>
